package lvgl_go

/*
#include "lv_init.h"
*/
import "C"

func Img(w uint, h uint, parent *LvObj) {
	// img := Create("img", parent)

	// if w == 0 {
	// 	w = uint(LV_SIZE_CONTENT)
	// }

	// if h == 0 {
	// 	w = uint(LV_SIZE_CONTENT)
	// }
}
